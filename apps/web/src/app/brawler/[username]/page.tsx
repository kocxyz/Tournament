export default async function BrawlerDetailsPage({ params: { username } }: { params: { username: string } }) {
  return <div className="flex-1 flex justify-center items-center">This Page is currently under development.</div>;
}
